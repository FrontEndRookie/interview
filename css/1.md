### 重排和重绘

```
重绘：元素外观改变，但元素的尺寸定位不变，不影响其他元素位置
重排：重新计算尺寸和布局，可能会影响其他元素位置，比重绘影响和消耗都大

减少重排：1.集中修改样式或直接修改class。2.修改之前设置display none脱离文档流。3.使用bfc特性，不影响其他元素位置。4.频繁触发使用节流和防抖。5.使用createDocumentFragment批量操作DOM
```

